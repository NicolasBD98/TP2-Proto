using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlackPaint : Collectible
{
    // Start is called before the first frame update
    void Start()
    {
        collectibleType = "blackPaint";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
