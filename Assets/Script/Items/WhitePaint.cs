using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WhitePaint : Collectible
{
    // Start is called before the first frame update
    void Start()
    {
        collectibleType = "whitePaint";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
